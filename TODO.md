# TODO

- [x] Спроектировать архитектуру
- [x] Спроектировать базу данных
- [x] Создать структуру проекта
- [x] Реализовать backend — все 4 способа отметки задач, чат, скриптованный диалог,
      звонки, ChatGPT-интеграция, контент-загрузчик (YAML), админ-эндпоинты. Подробности — `git log`.
- [x] Реализовать frontend — вход, задачи (все 4 способа, включая плавную анимацию
      разблокировки/выполнения), чат (оба режима), звонки. Не хватает: уведомлений о
      новых сообщениях (нужно сначала решить polling vs Realtime — см. память проекта).
- [x] Реализовать админку — вход, обзор/детали команды (+"точно?" подтверждение отметки),
      ручная проверка (комментарий доходит до чата техподдержки команды, этап 31), одобрение
      диалогов, чат оператора. Отдельная страница `/admin`. Не хватает: график прогресса
      команды как второй экран (упомянут в requirements.md, явно необязателен — "не основной
      экран").
- [x] Интеграция ChatGPT — backend готов; реальный вызов API намеренно не тестировался (решение пользователя, вернуться после деплоя остального).
- [x] Написать инструкцию для actor/operator по каждой вкладке админки — `docs/admin_guide.md`.
- [ ] Тестирование — включая нагрузочный тест с реальными людьми после деплоя (см. память проекта).
- [ ] Деплой — Render/GitHub, проверка тарифов Supabase, пинг-сервис для Render.
- [ ] Уведомления о новых сообщениях (toast) — требует сначала решить polling vs Realtime,
      см. память проекта. Пока команда видит новые сообщения (в т.ч. комментарии от
      проверки), только сама открыв вкладку "Чат".

Текущий прогресс — небольшими пронумерованными "этапами" (см. память проекта
`bestquest-project-overview` и CLAUDE.md про "инструкция как обычно").
На конец последней сессии: этап 31 закончен. Подробная история — `git log --oneline`.
