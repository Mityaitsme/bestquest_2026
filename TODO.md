# TODO

- [x] Спроектировать архитектуру
- [x] Спроектировать базу данных
- [x] Создать структуру проекта
- [x] Реализовать backend — все 4 способа отметки задач, чат, скриптованный диалог,
      звонки, ChatGPT-интеграция, контент-загрузчик (YAML), админ-эндпоинты. Подробности — `git log`.
- [x] Реализовать frontend — вход, задачи (все 4 способа, включая плавную анимацию
      разблокировки/выполнения), чат (оба режима, + toast-уведомления о новых сообщениях,
      этап 35), звонки (пароль цифрами и словом, этап 32; реальное аудио, этап 34).
- [x] Реализовать админку — вход, обзор/детали команды (+"точно?" подтверждение отметки,
      граф прогресса как второй экран), отдельные разделы "Техподдержка" и "Диалоги" —
      переписка от лица техподдержки/персонажей сразу по всем командам, с выбором персонажа
      (этап 36, вместо старого "чаты внутри карточки команды"), "Проверка" ручных ответов
      (доступна и оператору, и актёру, этап 36) с автосвязью комментария в чат команды
      (этап 31), "Блок-посты" — одобрение диалогов (только оператор). Персистентные баннеры
      с новыми заявками на проверку/блок-посты, висят до решения (этап 36). Красный кружок-
      индикатор на кнопках "Техподдержка"/"Проверка"/"Блок-посты" — горит, пока где-то есть
      неотвеченный запрос команды, виден обеим ролям (этап 37). Отдельная страница `/admin`.
- [x] Интеграция ChatGPT — backend готов; реальный вызов API намеренно не тестировался (решение пользователя, вернуться после деплоя остального).
- [x] Написать инструкцию для actor/operator по каждой вкладке админки — `docs/admin_guide.md`.
- [x] Уведомления о новых сообщениях (этап 35) — простой polling каждые 10 сек
      (`GET /chats/new-messages?since=...`), toast во всплывающем окне для любого чата, где
      появилось новое сообщение не от самой команды; если сейчас открыт именно этот чат — он
      тихо обновляется вместо тоста. Работает, пока сайт открыт в браузере; "получить, когда
      сайт был закрыт" не требует отдельной логики — команда и так увидит сообщение, открыв
      чат заново.
- [x] Реальное аудио в звонках (этап 34) — если рядом с YAML лежит файл `data/audio/<audio>`,
      он загружается в публичный Supabase Storage-бакет `call-audio` при импорте контента, и
      команда слышит настоящую запись (`<audio controls autoplay>`) вместо текста. Пока файла
      нет — работает как раньше, просто текст-заглушка. Ждём реальных аудиозаписей от автора
      контента — сама возможность готова.

**Из всего, что не относится к тестировке с реальными людьми, деплою и живому GPT API —
готово всё.** Дальше остаются только эти три пункта:
- [ ] Тестирование — включая нагрузочный тест с реальными людьми после деплоя (см. память проекта).
- [ ] Деплой — Render/GitHub, проверка тарифов Supabase, пинг-сервис для Render.
- [ ] Живой вызов ChatGPT API — backend готов, намеренно не тестировался (решение
      пользователя, вернуться после деплоя остального).

Текущий прогресс — небольшими пронумерованными "этапами" (см. память проекта
`bestquest-project-overview` и CLAUDE.md про "инструкция как обычно").
На конец последней сессии: этап 37 закончен. Подробная история — `git log --oneline`.
